/*
 * Create some sprite with retina sprite
 *
 */
module.exports = function (gulp, plugins) {
    return function () {
        gulp.task('sprite', function () {
			/*
			 * TODO : A Sprite task
			 * Idea: spritesmith
			 *       imageoptim
		 	 */
		 	return
 		});
 	}
}